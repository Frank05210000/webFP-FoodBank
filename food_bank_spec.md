# 食物銀行預訂網站 — 系統需求規格書

## 專題概述

### 核心概念
**食物銀行**是一個非營利性質的資源整合平台，由各界商家（超市、便利商店等）捐贈即將過期但仍可食用的食物，透過透明的線上預訂系統，將資源分配給有需要的民眾。本網站致力於「減少食物浪費、幫助經濟弱勢家庭」。

---

## 系統架構

### 用戶角色（3 個）
1. **民眾用戶** — 搜尋、預訂、取貨
2. **據點商家** — 登錄食物、管理訂單
3. **管理者（後台）** — 全局管理與數據分析

---

## 功能需求

### 📱 通用功能

#### 註冊/登入系統
- **註冊頁面**
  - 共同欄位：姓名、Email、電話、密碼
  - **據點商家需額外填寫：**
    - 商家名稱
    - 商家地址
    - 營業時間（開始時間、結束時間）
    - Google 地圖定位確認（使用者拖曳標記確認地點）
  - 驗證：Email 唯一、密碼強度檢查、電話格式驗證

- **登入方式**
  - Email + 密碼登入

---

### 👥 民眾用戶功能

#### 1️⃣ 首頁（主要搜尋與發現）
**功能需求：**
- 頁面載入時：自動請求用戶位置權限（Geolocation API）
- 地圖顯示（上半部）
  - 顯示用戶當前位置（藍點）
  - 標記所有食物銀行據點（紅點/自訂圖標）
  - 支援地圖縮放、拖曳
  
- 食物銀行清單（下半部，卡片式）
  - **兩種排序模式**（可切換）：
    - 「最近距離優先」— 根據用戶位置排序
    - 「20km 內物資最多」— 根據可預約食物總數排序
  
  - **卡片內容**（由上到下）：
    - 商家照片（預設圖片）
    - 商家名稱
    - 評分（★☆☆☆☆ ~ ★★★★★，滿分 5 星）
    - 地址、距離（例：2.3 km）
    - 開放時間（例：09:00 - 21:00）
    - **「預訂」按鈕**（大型，主色調）→ 連接至「商家頁面」

- **導航列**
  - 左側：網站名稱（Logo）
  - 右側：
    - 首頁
    - 預訂紀錄
    - 帳號管理
    - 登出

#### 2️⃣ 商家頁面（詳細商品清單）
**功能需求：**
- **上方欄位**
  - 商家名稱、評分、地址、開放時間

- **商品卡片清單**（中間主要區域）
  - 每張卡片：
    - 上方 60% — 食物照片（預設圖片）
    - 下方 40% — 文字資訊
      - 食物名稱
      - 可預約數量（例：「剩餘 5 份」）
      - **「加到購物車」按鈕**
  - 點擊後，數量選擇 → 加入購物車

- **購物車懸浮 Bar**（頁面下方固定）
  - 左側：顯示目前購物車商品數量（例：「3 件商品」）
    - 支援點擊展開/折疊
    - 展開時顯示所有品相、數量、可刪除
    - Bar 距離頁面邊緣保有 5% margin
  
  - 右側：**「結帳」按鈕**（主色調）→ 連接至「結帳頁面」

#### 3️⃣ 結帳頁面（確認與預訂）
**功能需求：**
- **上方區域 — 購物車清單**
  - 顯示所有商品
    - 商家名稱
    - 食物名稱 × 數量
    - 可刪除單項商品

- **中間區域 — 取貨設定**
  - 下拉式選單「選擇取貨時間」
    - 時間級距：30 分鐘（例：14:00, 14:30, 15:00...）
    - 限制條件：
      - 只能選擇 **現在之後** 的時間
      - 最晚時間 = 該商家營業結束時間
      - 範圍：今日內
  
  - **「確認預訂」按鈕**（主色調）

- **提交後 — 成功畫面**
  - 顯示打勾（✓ 或成功圖標）
  - 文案：「恭喜！您已成功預訂，請在 [時間] 前取貨」
  - 顯示訂單編號、預訂詳情
  - 返回首頁按鈕

#### 4️⃣ 預訂紀錄頁面
**功能需求：**
- 列表顯示所有預訂（新到舊排序）
- **每筆訂單卡片**（橫向卡片）
  - 左側 20% — 商家照片
  - 中間 60% — 訂單資訊
    - 訂單編號
    - 商家名稱
    - 取貨時間
    - 訂單內容品相清單（例：「飲料 × 2、食材 × 3」）
    - 訂單狀態（「待取貨」、「已取貨」、「已取消」）
  - 右側 20% — **「取消預訂」按鈕**（與卡片高度相同）
    - 灰顯若狀態為「已取貨」或「已取消」

#### 5️⃣ 帳號管理頁面
**功能需求：**
- 可編輯欄位：
  - 用戶名
  - Email
  - 電話
  - 密碼（修改密碼，需輸入舊密碼驗證）
- 按鈕：
  - 「保存更改」
  - 「登出」
  - 「刪除帳號」（需二次確認）

---

### 🏪 據點商家功能

#### 1️⃣ 商家首頁
**功能需求：**
- 顯示該商家的基本資訊（名稱、地址、營業時間）
- 快速統計面板
  - 今日預訂數
  - 待取貨訂單數
  - 本月累計貢獻食物份數
  - 平均評分

#### 2️⃣ 食物上架管理
**功能需求：**
- **新增食物**
  - 食物名稱
  - 食物類別（飲料、食材、便當、點心等）
  - 照片上傳（預設圖片如果沒上傳）
  - 可預約數量
  - 保存期限/過期時間
  - 簡單描述（例：「便利商店便當」）
  - 送出 → 自動上架至民眾可見清單

- **編輯/刪除食物**
  - 修改可預約數量（當有人預訂時自動扣減）
  - 標記「已售完」→ 民眾端該商品變灰顯、無法預訂
  - 刪除（若無未完成訂單則可刪除）

#### 3️⃣ 訂單管理
**功能需求：**
- 顯示該商家所有訂單（待取貨、已取貨、已取消）
- 每筆訂單顯示：
  - 訂單編號
  - 民眾名稱、電話
  - 預訂內容品相清單
  - 預計取貨時間
  - 目前狀態

- 操作：
  - 「標記為已取貨」（確認民眾已領取）
  - 「取消訂單」（若民眾未準時取貨）

#### 4️⃣ 帳號管理（同民眾版本）

---

### 🛠️ 後台管理功能

#### 1️⃣ 據點商家管理
**功能需求：**
- 新增店家
  - 手動輸入店家名稱、地址、電話、營業時間
  - 使用 Google 地圖 API 定位確認
  - 發送驗證碼給店家負責人 Email 確認身份
  - 驗證成功後帳號啟用

- 查看所有店家清單
  - 店家名稱、地址、註冊日期、帳號狀態
  
- 編輯店家資訊
  - 修改地址、營業時間、聯繫方式
  
- 刪除店家
  - 刪除前檢查是否有未完成訂單
  - 確認後刪除（包含已上架的食物）

#### 2️⃣ 民眾帳號管理
**功能需求：**
- 查看所有民眾清單
  - 用戶名、Email、電話、註冊日期
  
- 搜尋/篩選
  - 按用戶名搜尋
  - 按註冊日期範圍篩選
  
- 刪除民眾帳號
  - 刪除前檢查是否有未完成訂單
  - 確認後刪除帳號及其預訂紀錄

#### 3️⃣ 訂單管理
**功能需求：**
- 查看全系統訂單
  - 訂單編號、民眾名稱、商家名稱、預訂時間、狀態
  
- 搜尋/篩選
  - 按商家篩選
  - 按狀態篩選（待取貨、已取貨、已取消）
  - 按日期範圍篩選

- 操作（若需要干預）
  - 編輯訂單狀態
  - 刪除訂單紀錄

#### 4️⃣ 數據分析與統計
**功能需求：**
- **全局統計面板**
  - 累計預訂數
  - 累計減少浪費食物份數
  - 累計幫助民眾數
  - 月度趨勢圖表
  
- **商家排行榜**
  - 貢獻食物數最多的商家
  - 預訂數最多的商家
  - 平均評分最高的商家

- **月度報告**
  - 各月份的預訂統計
  - 各月份的食物貢獻統計
  - 可導出 CSV 或 PDF

- **用戶活躍度**
  - 日均預訂用戶數
  - 商家上架食物數趨勢

---

## 資料庫設計概要 (Database Schema)

對應作業需求：
- **Users** → `users` (民眾), `shops` (商家)
- **Services** → `foods` (食物/物資)
- **Bookings** → `orders` (訂單)

### 主要資料表
```
users (民眾)
├─ user_id (PK)
├─ name
├─ email (unique)
├─ phone
├─ password_hash
├─ location (lat, lng) [可選，需要時更新]
├─ created_at
└─ deleted_at [軟刪除]

shops (商家據點)
├─ shop_id (PK)
├─ shop_name
├─ manager_email (unique)
├─ phone
├─ address
├─ lat, lng [地圖定位]
├─ opening_time, closing_time
├─ rating [平均評分]
├─ created_at
└─ deleted_at

foods (食物清單)
├─ food_id (PK)
├─ shop_id (FK)
├─ name
├─ category
├─ quantity [可預約數量]
├─ expiry_time
├─ photo_url
├─ description
├─ created_at
└─ deleted_at

orders (訂單)
├─ order_id (PK)
├─ user_id (FK)
├─ shop_id (FK)
├─ pickup_time
├─ status [待取貨/已取貨/已取消]
├─ created_at
└─ completed_at

order_items (訂單品項)
├─ item_id (PK)
├─ order_id (FK)
├─ food_id (FK)
├─ quantity
└─ unit_price [記錄預訂時的數量快照]
```

---

## 技術堆棧 (Technical Stack)

### 前端 (Frontend)
- **核心技術：** HTML, CSS, JavaScript
- **UI 框架：** Bootstrap 5 (採用簡約風格設計)
- **地圖整合：** Leaflet.js + OpenStreetMap (輕量化替代 Google Maps)

### 後端 (Backend)
- **框架：** Flask (Python)
- **資料庫：** PostgreSQL
- **ORM 工具：** SQLAlchemy

### 部署與環境 (Deployment)
- **託管服務：** Render / Heroku / AWS (擇一)
- **版本控制：** Git / GitHub
- **安全性：** 基礎 Input Validation 與 HTTPS (部署時配置)

---

## 開發階段規劃 (Development Phases)

### 1. 規劃與設計 (Planning & Design)
- 繪製 UI 草圖 (Wireframes) - 採簡約風格
- 設計資料庫架構 (Schema Design)
- 確定 API 路由規劃

### 2. 核心開發 (Development)
- **環境建置：** 設定 Flask 與 PostgreSQL 連線
- **後端開發：** 實作使用者認證、商家管理、訂單處理 API
- **前端整合：** 整合 Bootstrap 介面與 Leaflet 地圖

### 3. 測試 (Testing)
- 單元測試 (Unit Tests)：針對資料庫模型與 API
- 使用者流程測試：註冊、預訂、取消流程驗證

### 4. 部署 (Deployment)
- 部署至雲端平台 (如 Render/Heroku)
- 設定環境變數與資料庫遷移

### 5. 文件撰寫 (Documentation)
- 系統安裝指南 (Setup Guides)
- API 文件與程式碼說明
- 使用者操作手冊

## 交付項目 (Deliverables)
1. **原始碼 (Source Code)**：包含前端與後端完整程式碼
2. **資料庫腳本 (Database Scripts)**：Schema 建立腳本 (SQL 或 Migration files)
3. **專案文件 (Documentation)**：README.md (含安裝與執行教學)
4. **展示 (Demo)**：部署連結或操作演示影片

---

## 備註

- **評分系統** — 民眾取貨後可留下 1-5 星評價（建議放入 Phase 2）
- **通知機制** — 可考慮在 Phase 2 加入 Email 通知（訂單確認、即將過期提醒等）
- **圖片管理** — 建議使用雲端儲存（如 AWS S3、Google Cloud Storage）或本地文件系統
- **負載測試** — 若預期同時在線用戶較多，需提前規劃資料庫索引與快取策略
